# migrations 001–005 — เลิกใช้แล้ว (pre-v2)

ไฟล์ในโฟลเดอร์นี้เป็นการแก้ฐานข้อมูล **โครงเก่า** ทีละขั้น ตอนที่ยังไม่มีชั้น
`clubs` และ `sessions` ยังหมายถึง "ก๊วน" แทนที่จะเป็น "วันเล่น"

ตั้งแต่ v2 (2026-09-12) เป็นต้นไป ฐานข้อมูลถูกสร้างใหม่ทั้งหมดจาก
[`../schema.sql`](../schema.sql) + [`../functions.sql`](../functions.sql)
**อย่ารันไฟล์ในโฟลเดอร์นี้กับฐานใหม่** — เก็บไว้อ้างอิงประวัติเท่านั้น

การแก้ฐานข้อมูลหลังจากนี้ให้เริ่มนับใหม่ที่ `101_*.sql` และต้องแก้
`schema.sql` / `functions.sql` ให้ตรงกันเสมอ
